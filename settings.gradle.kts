pluginManagement {
    repositories {
        maven(url="https://maven.java.net/content/repositories/public/")
        mavenCentral()
        jcenter()
        mavenLocal()
        gradlePluginPortal()
        maven(url = "https://dl.bintray.com/kotlin/kotlin-dev" )
    }
}

rootProject.name = "smartlexer"
