AreaB
{{#each tokens}}
 {{#if (isFirst @index)}}={{else}}/{{/if}} {{titleCase name}}Space
{{/each}}

{{#each tokens}}
{{titleCase name}}Space "correct {{name}} column"
 = spaces:" "* &{ return spaces.length === {{column}}; }

{{/each}}